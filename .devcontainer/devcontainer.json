{
  "name": "saas-handson-devcontainer",
  "dockerComposeFile": "./docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces",
  "shutdownAction": "stopCompose",
  "customizations": {
    "vscode": {
      "extensions": [
        "haskell.haskell",
        "justusadam.language-haskell",
        "EditorConfig.EditorConfig",
        "anthropic.claude-code",
        "growthjack.claude-code-usage"
      ],
      "settings": {
        "haskell.manageHLS": "GHCup",
        "editor.formatOnSave": false,
        "files.watcherExclude": {
          "**/dist-newstyle/**": true
        }
      }
    }
  },
  "initializeCommand": "mkdir -p \"${localWorkspaceFolder}/.devcontainer/claude-home/.claude\"",
  "mounts": [
    "source=${localWorkspaceFolder}/.devcontainer/claude-home/.claude,target=/root/.claude,type=bind"
  ],
  "containerEnv": {
    "CLAUDE_CONFIG_DIR": "/root/.claude"
  },
  "postCreateCommand": "cabal update"
}
